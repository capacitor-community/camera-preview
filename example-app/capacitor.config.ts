import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ryltsov.alex.camera.preview.demo',
  appName: 'example-app',
  webDir: 'www/browser'
};

export default config;
